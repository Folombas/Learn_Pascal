# Артефакты Free Pascal
*.o
*.ppu
*.a
*.so

# Бинарники — компилируются из .pas и имеют то же имя без расширения.
# Явно перечислим возможные имена, либо используйте исключение:

# Всё игнорировать, кроме .pas и самого .gitignore:
*
!.gitignore
!*.pas
!*.md
!Makefile
